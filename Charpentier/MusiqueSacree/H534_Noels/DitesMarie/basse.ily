\clef "basse" re1._\markup\orig-version\whiteout "acc seul"
^\markup\mod-version\whiteout "acc seul" ~ |
re2^\markup\orig-version \musicglyph #"scripts.segno" fa
<<
  \origVersion re_\markup\whiteout "tous fort"
  ^\markup\musicglyph #"scripts.segno"
  \modVersion re^\markup\whiteout\center-align "Tous" \f >> |
la1 la2 |
mi2. fa8*2 sol2 |
re re'8*2 do' sib la |
sol1 la2 |
sib sol la |
re1 re2_\markup\orig-version\whiteout "acc seul sourdines"
^\markup\mod-version\whiteout "acc seul"
_\markup\mod-version\whiteout "sourdines" |
dod1. |
do! |
si,1 sib,2 |
la, sib, la, |
sol, la, fa, |
sol, mi, la, |
re,1 sol,2~ |
<<
  \origVersion { sol,1 sol2_\markup\orig-version\whiteout "tous fort" }
  \modVersion { sol,1*3/4 s4\f sol2^\markup\whiteout\center-align "Tous" } >> |
la1. |
la2 la la |
sib1. |
la2 sol fa |
mi1 re2_\markup\orig-version\whiteout "acc seul sourdines"
^\markup\mod-version\whiteout "acc seul sourdines" |
dod2. re8*2 sol,2 |
la,1. |
la,2 la, la, |
sib,2. la,8*2 sol,2~ |
sol, la,1 |
re,1.~ |
re,1 <<
  \origVersion re2_\markup\whiteout "tous fort"
  \modVersion re2^\markup\whiteout\center-align "Tous" \f >> |
la1 la2 |
mi2. fa8*2 sol2 |
re2. mi8*2 fa2 |
mi la fa |
sol sib sol |
re'2. do'8*2_\markup\orig-version\whiteout "tous sourdines"
^\markup\mod-version\whiteout "Tous"
_\markup\mod-version\whiteout "sourdines" sib2 |
la8*2 sol fa mi re2 |
dod1 do2 |
si,1 sib,2 |
la,1 sib,2 |
sol, la, fa, |
sol, fa, mi, |
re,1 <<
  \origVersion re2_\markup\whiteout "tous fort"
  \modVersion re2^\markup\whiteout\center-align "Tous" \f >> |
la,1 mi2 |
fa sol2. dod8*2 |
re1 mi2 |
fa2 si, dod |
re sol1 |
la8*2_\markup\orig-version\whiteout "tous sourdines"
^\markup\mod-version\whiteout "Tous sourdines" sib sol la fa sol |
mi re mi fa mi re |
dod re la,2. la8*2 |
re1 la2 |
sib sol mi |
fa sol la |
re <<
  \origVersion re'2._\markup\whiteout "fort"
  \modVersion re'2.\f >> do'8*2 |
sib2 sib la |
sol do' do |
fa2. mi8*2 fa2 |
sol1.~ |
sol2 la8*2 sol fa mi |
re sol, la,1 |
<<
  \origVersion { re1 }
  \modVersion {
    re1~ re2*3/4^\markup\mod-version\whiteout\center-align "acc seul" ~
    \hideNotes re8 \unHideNotes |
    re1
  }
>>
